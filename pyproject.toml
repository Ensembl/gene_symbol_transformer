[tool.poetry]
name = "gene_symbol_classifier"
version = "0.1.0"
description = ""
authors = ["Dimitrios Paraschas <dimitrios@ebi.ac.uk>"]

[tool.poetry.dependencies]
python = "^3.8"
pandas = "^1.0.4"
biopython = "^1.77"
torch = "^1.5.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
